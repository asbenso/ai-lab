#!/usr/bin/env bash
# Run Monk bootcamp commands from the AgenticAi directory.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT="$ROOT/Research-Assistant"
exec make -C "$PROJECT" "$@"
