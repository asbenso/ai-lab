#!/usr/bin/env bash
# Monk CLI — delegates make targets into Research-Assistant/.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT="$ROOT/Research-Assistant"
exec make -C "$PROJECT" "$@"
