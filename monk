#!/usr/bin/env bash
# Monk CLI — run make targets from anywhere in the repo.
# Usage: ./monk <target>   e.g.  ./monk tools   ./monk graph-fake
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$ROOT/AiProjects/AgenticAi/monk" "$@"
